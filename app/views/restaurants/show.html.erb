<h1>Restaurants#show</h1>
<p>Find me in app/views/restaurants/show.html.erb</p>

<h1><%= @restaurant.name %> <small><%= @restaurant.address %></small></h1>
<h2>
  <%= pluralize @restaurant.reviews.size, "review" %>
</h2>

<div id="reviews">
  <% if @restaurant.reviews.blank? %>
    Be the first to leave a review for <%= @restaurant.name %>
  <% else %>
    <% @restaurant.reviews.each do |review| %>
      <%= render 'reviews/show', review: review %>
    <% end %>
  <% end %>
</div>


<%= render 'reviews/form', restaurant: @restaurant, review: @review %>
<!--
render in that case is injecting the form simple_form_for to add a review passing the @restaurant from the action show in my restaurants controller andf the empty object review from show restaurants controller -->
<%#= simple_form_for [mango, apple] do |f| %>
<%#= f.input :content %>
<%#= f.submit %>
<%# end %>
